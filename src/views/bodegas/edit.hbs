<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto ">
            <div class="card ">
                <div class="card-body">
                    <form action="/bodegas/edit/{{bodega.id}}" method="POST">
                        <div class="form-group">
                            <input type="text" value="{{bodega.name}}" class="form-control mt-2" name="name" placeholder="Title" autofocus >
                        </div>

                        <div class="form-group">
                            <input type="number" value="{{bodega.headquarters_number}}" class="form-control mt-2" name="headquarters_number" placeholder="Head">
                        </div>

                        <div class="form-group">
                            <input type="number" value="{{bodega.phone}}" class="form-control mt-2" name="phone" placeholder="Telefono">
                        </div>

                        <div class="form-group">
                            <input type="text" value="{{bodega.city}}" class="form-control mt-2" name="city" placeholder="Ciudad">
                        </div>

                        <div class="form-group">
                            <input type="text" value="{{bodega.address}}" class="form-control mt-2" name="address" placeholder="Dirección">
                        </div>

                        <div class="form-group">
                            <button class="btn btn-success btn-block mt-2 " type="submit">
                                Guardar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>